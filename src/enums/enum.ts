export enum LoadingStatus {
	Pedding = 'pendding',
	Fulfilled = 'fulfilled',
	Rejected = 'rejected',
}

export enum EPrioritySuport {
	High, // red
	Medium, // yellow
	Low, //green
}

export enum EResloved {
	Done,
	Pendding,
	Cancel,
}
